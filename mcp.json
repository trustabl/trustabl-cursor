{
  "mcpServers": {
    "trustabl": {
      "command": "node",
      "args": ["${PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "TRUSTABL_VERSION": "${TRUSTABL_VERSION}",
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
