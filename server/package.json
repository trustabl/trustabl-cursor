{
  "name": "trustabl-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing the Trustabl agent-safety scanner as a tool",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "esbuild index.js --bundle --platform=node --target=node18 --format=esm --outfile=../dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
